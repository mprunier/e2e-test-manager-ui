apiVersion: v1
kind: Service
metadata:
  name: e2e-testing-manager-ui
  namespace: test-e2e
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 80
  selector:
    app: e2e-testing-manager-ui
